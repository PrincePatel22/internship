const filter = (e) => { let keyword; if (e) { keyword = e.target.value; } else { keyword = ""; } if (keyword !== '') { const results = Disp.filter((user) => { return user.firstname.toLowerCase().startsWith(keyword.toLowerCase()); }); SetSearchDisp(results); } else { SetSearchDisp(Disp); } setName(keyword); };